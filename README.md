# TrustWallet Assets API

> Simple Vercel serverless function to get token assets from the TrustWallet ecosystem.

## Usage

* `yarn`
* `yarn vercel dev`

Open your browser at `http://localhost:3000`
